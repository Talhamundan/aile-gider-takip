---
description: Update the GitHub repository with local changes
---
1. Stage all changes
   `git add .`

2. Commit changes (Replace "Update" with your own message)
   `git commit -m "Update"`

3. Push changes to GitHub
   `git push`
